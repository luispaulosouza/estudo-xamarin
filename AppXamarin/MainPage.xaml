<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:AppXamarin" x:Class="AppXamarin.MainPage"
             Padding="0,20,0,0">
    <StackLayout>

        
        <Entry Placeholder="Login" Text="{Binding Login, Mode=TwoWay }"> </Entry>
        <Entry Placeholder="Senha" Text="{Binding Senha}"> </Entry>
        <Button Text="Logar" Command="{Binding Logar}" ></Button>

    </StackLayout>
</ContentPage>